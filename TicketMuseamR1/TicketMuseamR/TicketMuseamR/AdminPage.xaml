<Page x:Class="TicketMuseamR.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="1000"
      Title="AdminPage"
      Loaded="Page_Loaded">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10,10,0,20">
            <TextBlock Text="PANEL DE ADMINISTRACIÓN" FontWeight="Bold" FontSize="36" Foreground="#005A9E"/>
            <TextBlock Text="Filtros por día, control de caja y auditoría de TB_VENTAS y TB_VISITANTES" FontFamily="Arial" FontSize="14" Foreground="#666666" Margin="2,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#F0F4F8" CornerRadius="8" Padding="15,10" Margin="10,0,10,20" BorderBrush="#D0D7DE" BorderThickness="1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="📅 Filtrar Rango - Desde:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,10,0"/>
                <DatePicker x:Name="dpDesde" Width="150" Height="28" VerticalAlignment="Center" SelectedDateChanged="FiltroFecha_SelectedDateChanged"/>

                <TextBlock Text="Hasta:" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#333333" Margin="20,0,10,0"/>
                <DatePicker x:Name="dpHasta" Width="150" Height="28" VerticalAlignment="Center" SelectedDateChanged="FiltroFecha_SelectedDateChanged"/>

                <Button x:Name="BtnHoy" Content="Hoy" Background="#005A9E" Foreground="White" FontWeight="Bold" Width="70" Height="28" Margin="20,0,0,0" Cursor="Hand" Click="BtnHoy_Click"/>
            </StackPanel>
        </Border>

        <UniformGrid Grid.Row="2" Columns="2" Height="110" Margin="10,0,10,20">
            <Border Background="#FFFFFF" CornerRadius="12" Margin="10,0,10,0" BorderBrush="#E0E0E0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Color="#CCCCCC"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="#107C41" CornerRadius="12,0,0,12"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="25,0,0,0">
                        <TextBlock Text="RECAUDADO EN EL RANGO" FontFamily="Segoe UI" FontWeight="Bold" FontSize="12" Foreground="#777777"/>
                        <TextBlock x:Name="lblTotalRecaudado" Text="$ 0.00" FontWeight="Bold" FontSize="26" Foreground="#107C41" Margin="0,3,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="#FFFFFF" CornerRadius="12" Margin="10,0,10,0" BorderBrush="#E0E0E0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Color="#CCCCCC"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Background="#005A9E" CornerRadius="12,0,0,12"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="25,0,0,0">
                        <TextBlock Text="BOLETOS EMITIDOS EN EL RANGO" FontWeight="Bold" FontSize="12" Foreground="#777777"/>
                        <TextBlock x:Name="lblTotalBoletos" Text="0 Boletos" FontWeight="Bold" FontSize="26" Foreground="#005A9E" Margin="0,3,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="3" Margin="10,0,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <Button x:Name="BtnCorteCaja" Content="🔒  Hacer Corte de Caja (Hoy)" Background="#8B0000" Foreground="White" 
                        FontWeight="Bold" FontSize="14" Height="35" Width="230" BorderThickness="0" Cursor="Hand" Click="BtnCorteCaja_Click"/>
            </StackPanel>

            <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="12" BorderBrush="#E0E0E0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" Direction="270" ShadowDepth="3" Color="#DDDDDD"/>
                </Border.Effect>
                <Grid Margin="15">
                    <DataGrid x:Name="dgVentas" AutoGenerateColumns="False" IsReadOnly="True" 
                              Background="Transparent" BorderThickness="0" RowBackground="#FFFFFF" 
                              AlternatingRowBackground="#F9F9F9" GridLinesVisibility="Horizontal" 
                              HorizontalGridLinesBrush="#E0E0E0" FontSize="14" Foreground="#333333" RowHeight="35">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#005A9E"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="10,8,10,8"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID Venta" Binding="{Binding Id_venta}" Width="100" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Monto Cobrado" Binding="{Binding Monto_total, StringFormat={}{0:C}}" Width="180"/>
                            <DataGridTextColumn Header="Método de Pago" Binding="{Binding Metodo_pago}" Width="200"/>
                            <DataGridTextColumn Header="Fecha y Hora de Operación" Binding="{Binding Fecha_venta, StringFormat=\{0:dd/MM/yyyy hh:mm:tt\}}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
