<Page x:Class="TicketMuseamR.BitacoraPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="BitacoraPage"
      Loaded="Page_Loaded">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="10,10,0,20">
            <TextBlock Text="BITÁCORA DE VISITAS" FontWeight="Bold" FontSize="36" Foreground="#005A9E"/>
            <TextBlock Text="Historial y registro de accesos en tiempo real de TB_VISITANTES" FontFamily="Arial" FontSize="14" Foreground="#666666" Margin="2,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,0,0,20" VerticalAlignment="Center">
            <Button x:Name="BtnActualizar" Content="🔄  Actualizar Tabla" Background="#005A9E" Foreground="White" 
                    FontWeight="Bold" FontSize="14" Height="35" Width="160" BorderThickness="0" Cursor="Hand" Click="BtnActualizar_Click"/>

            <TextBlock Text="🔍 Buscar Responsable:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Foreground="#333333" Margin="40,0,10,0"/>
            <TextBox x:Name="txtBuscar" Width="280" Height="28" VerticalContentAlignment="Center" Padding="5,0" FontSize="14" 
                     BorderBrush="#D0D7DE" BorderThickness="1" TextChanged="txtBuscar_TextChanged"/>
        </StackPanel>

        <Border Grid.Row="2" Background="#FFFFFF" CornerRadius="12" BorderBrush="#E0E0E0" BorderThickness="1" Margin="10,0,10,10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="270" ShadowDepth="3" Color="#CCCCCC"/>
            </Border.Effect>

            <Grid Margin="15">
                <DataGrid x:Name="dgVisitantes" AutoGenerateColumns="False" IsReadOnly="True" 
                          Background="Transparent" BorderThickness="0" RowBackground="#FFFFFF" 
                          AlternatingRowBackground="#F9F9F9" GridLinesVisibility="Horizontal" 
                          HorizontalGridLinesBrush="#E0E0E0" SelectionMode="Single"
                          FontSize="14" Foreground="#333333" RowHeight="35">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#005A9E"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,8,10,8"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID Registro" Binding="{Binding Id_visita}" Width="120" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Nombre del Responsable" Binding="{Binding Nom_visita}" Width="*"/>
                        <DataGridTextColumn Header="Cantidad de Boletos" Binding="{Binding Cant_bol}" Width="180"/>
                        <DataGridTextColumn Header="Fecha y Hora de Entrada" Binding="{Binding hora_ent, StringFormat=\{0:dd/MM/yyyy hh:mm:tt\}}" Width="250"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>